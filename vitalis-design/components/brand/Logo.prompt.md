Logo — the Vitalis brand mark (a soft shield holding the Vorsorge-Ring) with an optional wordmark. SVG, scales cleanly.

<Logo size={46} wordSize={30} />           // mark + wordmark
<Logo size={20} word={false} />            // icon only (headers, tiles)
<Logo reversed />                          // white, for dark/blue backgrounds

Icon-only mark doubles as the app icon (on a primary-blue rounded tile — see guidelines/logo/vitalis-appicon.svg).
