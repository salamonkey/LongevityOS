ListRow — the workhorse item for tasks, vaccinations, screenings, appointments, and documents. Icon chip on the left, title + subtitle, trailing status badge or chevron.

<ListRow icon="syringe" tone="primary" title="Grippeimpfung" subtitle="Auffrischung fällig" badge="Fällig" badgeStatus="due" />

Use `tone` to color-code the icon chip by category. Omit `badge` to show a chevron for navigation.
