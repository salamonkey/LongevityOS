Card — the base white surface. Wrap any grouped content (score, summary, form section).

<Card padding={18}>…content…</Card>

Set `elevated={false}` for a flat card on white; pass `onClick` to make it pressable.
