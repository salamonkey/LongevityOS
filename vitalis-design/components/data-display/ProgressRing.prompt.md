ProgressRing — the signature Vorsorge-Score donut. Animates its sweep on mount.

<ProgressRing value={72} size={80} label="72%" sublabel="Score" />

Override `color` for category-specific rings. Keep it as the single hero metric per screen.
