Input — labelled single-line text field. Shows a focus ring and supports `icon`, `hint`, and `error`.

<Input label="E-Mail" icon="mail" placeholder="anna@example.de" hint="Wir senden dir Erinnerungen." />

Pass `error` to turn the field red and replace the hint.
