IconButton — circular icon-only action for headers and toolbars. Always pass `label` for accessibility.

<IconButton icon="bell" variant="surface" label="Benachrichtigungen" />

Variants: `soft`, `solid`, `ghost`, `surface` (white with shadow, e.g. floating header buttons).
