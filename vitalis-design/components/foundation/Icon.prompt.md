Icon — the single glyph API used across Vitalis. Renders a Lucide line icon.

<Icon name="shield-check" size={24} color="var(--color-primary)" />

Pass any Lucide name. Defaults to 20px / 1.75 stroke. Requires the Lucide UMD script loaded on the page (the app shell does this once).
